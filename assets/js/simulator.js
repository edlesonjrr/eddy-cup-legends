const wait=ms=>new Promise(resolve=>setTimeout(resolve,ms));
export const MATCH_DURATION_MS=90000;
export const EXTRA_TIME_MS=10000;
const hash=value=>[...value].reduce((acc,char)=>(acc*31+char.charCodeAt(0))>>>0,2166136261);
const rngFrom=seed=>{let state=hash(seed);return()=>{state=(state*1664525+1013904223)>>>0;return state/4294967296}};
const groups={defense:new Set(['GK','RB','LB','CB']),midfield:new Set(['CDM','CM','CAM','RM','LM']),attack:new Set(['RW','LW','CF','ST'])};
const average=list=>list.length?list.reduce((sum,item)=>sum+item.overall,0)/list.length:70;
export function teamProfile(team){const by=group=>team.filter(player=>groups[group].has(player.position));return{overall:average(team),goalkeeper:average(by('defense').filter(player=>player.position==='GK')),defense:average(by('defense')),midfield:average(by('midfield')),attack:average(by('attack'))}}
const attackers=team=>[...team].filter(player=>groups.attack.has(player.position)||player.position==='CAM').sort((a,b)=>b.overall-a.overall);
const eventText=(minute,text)=>`${minute}' — ${text}`;

export async function runMatch({home,away,seed='legends',onUpdate,onDecision,durationMs=MATCH_DURATION_MS,extraTimeMs=EXTRA_TIME_MS}){
  const random=rngFrom(seed),profile={home:teamProfile(home),away:teamProfile(away)},competitiveMatch=Math.abs(profile.home.overall-profile.away.overall)<3;
  const state={phase:'first-half',minute:0,score:{home:0,away:0},stats:{home:{shots:0,saves:0,cards:0,possession:50},away:{shots:0,saves:0,cards:0,possession:50}},logs:[],momentum:{home:0,away:0},tactics:{home:'balanced',away:'balanced'},finished:false};
  const emit=async()=>onUpdate?.(structuredClone(state));
  const log=async text=>{state.logs.push(text);if(state.logs.length>40)state.logs.shift();await emit()};
  const strength=side=>{const rival=side==='home'?'away':'home',tactic=state.tactics[side],bonus=tactic==='attack'?3:tactic==='defend'?-2:0;return profile[side].attack*.38+profile[side].midfield*.32+profile[side].overall*.3-profile[rival].defense*.22+bonus+state.momentum[side]*2};
  const attackEvent=async(side,minute)=>{const rival=side==='home'?'away':'home',team=side==='home'?home:away,opponent=rival==='home'?home:away,player=attackers(team)[Math.floor(random()*Math.max(1,attackers(team).length))]||team[0],keeper=opponent.find(item=>item.position==='GK')||opponent[0];state.stats[side].shots++;const base=Math.max(.07,Math.min(.38,.14+(strength(side)-55)/180)),goalChance=competitiveMatch?base*.78:base;if(random()<goalChance){state.score[side]++;state.momentum[side]=0;state.momentum[rival]-=.3;await log(eventText(minute,`GOL! ${player.name} finaliza com precisão e marca!`))}else if(random()<.58){state.stats[rival].saves++;state.momentum[side]+=.22;await log(eventText(minute,`${player.name} bate forte, mas ${keeper.name} faz a defesa.`))}else{state.momentum[side]+=.12;await log(eventText(minute,`${player.name} finaliza para fora por muito pouco.`))}};
  const expectedTicks=Math.max(12,Math.floor(durationMs/750)),randomTick=()=>5+Math.floor(random()*Math.max(4,expectedTicks-10));
  const penaltyTick=random()<.18?randomTick():null,injuryTick=random()<.12?randomTick():null,redTick=random()<.06?randomTick():null,quickTick=random()<.72?randomTick():null;
  let tick=0,halftimeDone=false,penaltyDone=false,injuryDone=false,redDone=false,quickDone=false;
  await log(eventText(0,'A bola está rolando no Estádio das Lendas.'));const started=Date.now();
  while(Date.now()-started<durationMs){
    await wait(Math.min(750,durationMs));tick++;state.minute=Math.min(90,Math.floor((Date.now()-started)/durationMs*90));state.phase=state.minute<45?'first-half':'second-half';
    if(!halftimeDone&&state.minute>=45){halftimeDone=true;await log(eventText(45,'Intervalo. Os treinadores preparam os ajustes.'));for(const side of ['home','away']){const choice=await onDecision?.({type:'tactic',side,options:['attack','balanced','defend'],timeout:8000,state:structuredClone(state)});state.tactics[side]=choice?.value||'balanced'}await log(eventText(46,'Começa o segundo tempo.'))}
    const attacking=random()<.5?'home':'away',defending=attacking==='home'?'away':'home';state.stats.home.possession=Math.max(38,Math.min(62,Math.round(50+(profile.home.midfield-profile.away.midfield)*.55+(state.momentum.home-state.momentum.away)*2)));state.stats.away.possession=100-state.stats.home.possession;
    if(!quickDone&&quickTick&&tick>=quickTick){quickDone=true;const choice=await onDecision?.({type:'quick',side:attacking,options:['press','wings','possession'],timeout:8000,state:structuredClone(state)}),value=choice?.value||'possession';if(value==='press'){state.momentum[attacking]+=.65;state.tactics[attacking]='attack';await log(eventText(state.minute,'Decisão rápida: pressão alta perto da área.'))}else if(value==='wings'){state.momentum[attacking]+=.45;await log(eventText(state.minute,'Decisão rápida: o time passa a explorar as pontas.'))}else{state.momentum[attacking]+=.3;state.tactics[attacking]='balanced';await log(eventText(state.minute,'Decisão rápida: posse paciente para controlar o ritmo.'))}continue}
    if(!penaltyDone&&penaltyTick&&tick>=penaltyTick){penaltyDone=true;const team=attacking==='home'?home:away,candidates=attackers(team).slice(0,3),opponent=defending==='home'?home:away,keeper=opponent.find(item=>item.position==='GK')||opponent[0],decision=await onDecision?.({type:'penalty',side:attacking,candidates,options:['left','center','right'],timeout:10000,state:structuredClone(state)}),batter=candidates.find(item=>item.id===decision?.batterId)||candidates[0],direction=decision?.value||['left','center','right'][Math.floor(random()*3)],keeperDirection=['left','center','right'][Math.floor(random()*3)],base=Math.max(.45,Math.min(.9,.72+((batter?.overall||80)-keeper.overall)*.012)),goal=random()<(direction===keeperDirection?base*.4:base);await log(eventText(state.minute,`Pênalti! ${batter.name} contra ${keeper.name}...`));if(goal){state.score[attacking]++;await log(eventText(state.minute,`GOL DE PÊNALTI! ${batter.name} marca.`))}else{state.stats[defending].saves++;await log(eventText(state.minute,`DEFENDEU! ${keeper.name} salva.`))}continue}
    if(!injuryDone&&injuryTick&&tick>=injuryTick){injuryDone=true;const player=(attacking==='home'?home:away)[Math.floor(random()*11)];state.momentum[attacking]-=.35;await log(eventText(state.minute,`${player.name} sente uma pancada, recebe atendimento e continua.`));continue}
    if(!redDone&&redTick&&tick>=redTick){redDone=true;state.stats[defending].cards++;state.momentum[defending]-=.7;await log(eventText(state.minute,'Cartão vermelho! Um jogador é expulso.'));continue}
    const roll=random();if(roll<.14)await attackEvent(attacking,state.minute);else if(roll<.20){state.stats[defending].cards++;await log(eventText(state.minute,'Falta dura. Cartão amarelo.'))}else if(roll<.24){state.stats[attacking].shots++;state.momentum[attacking]+=.18;await log(eventText(state.minute,'A finalização explode na trave!'))}else if(roll<.30)await log(eventText(state.minute,`${attacking==='home'?'O time da casa':'O visitante'} aumenta a pressão.`));state.momentum.home*=.92;state.momentum.away*=.92;await emit()
  }
  state.minute=90;await log(eventText(90,'Fim do tempo regulamentar.'));
  if(state.score.home===state.score.away){await interactiveShootout({home,away,state,profile,random,onDecision,emit,log,extraTimeMs,competitiveMatch})}
  state.finished=true;state.phase='finished';await emit();return state
}

async function interactiveShootout({home,away,state,random,onDecision,emit,log,extraTimeMs,competitiveMatch}){
  state.phase='extra-time';await log(competitiveMatch?'PRORROGAÇÃO — O equilíbrio aumenta a chance de pênaltis.':'PRORROGAÇÃO — Ainda está tudo igual.');await wait(extraTimeMs);state.phase='shootout';await log('PÊNALTIS — Escolha a ordem dos cobradores e os cantos.');
  const ordered={};
  for(const side of ['home','away']){const team=side==='home'?home:away,candidates=[...team].filter(player=>player.position!=='GK').sort((a,b)=>b.overall-a.overall).slice(0,8),decision=await onDecision?.({type:'shootout-order',side,candidates,timeout:15000,state:structuredClone(state)}),ids=decision?.orderedIds||[];ordered[side]=[...ids.map(id=>candidates.find(player=>player.id===id)).filter(Boolean),...candidates.filter(player=>!ids.includes(player.id))].slice(0,5)}
  let homePens=0,awayPens=0;
  for(let shot=0;shot<5;shot++)for(const side of ['home','away']){const rival=side==='home'?'away':'home',batter=ordered[side][shot],opponent=rival==='home'?home:away,keeper=opponent.find(player=>player.position==='GK')||opponent[0],decision=await onDecision?.({type:'shootout-kick',side,candidates:[batter],shot:shot+1,options:['left','center','right'],timeout:9000,state:structuredClone(state)}),direction=decision?.value||['left','center','right'][Math.floor(random()*3)],keeperDirection=['left','center','right'][Math.floor(random()*3)],base=Math.max(.42,Math.min(.9,.7+(batter.overall-keeper.overall)*.012)),goal=random()<(direction===keeperDirection?base*.38:base);if(goal){side==='home'?homePens++:awayPens++;await log(`PÊNALTI ${shot+1} — GOL! ${batter.name} vence ${keeper.name}.`)}else{await log(`PÊNALTI ${shot+1} — ${keeper.name} defende a cobrança de ${batter.name}!`)}state.shootout={home:homePens,away:awayPens};await emit()}
  while(homePens===awayPens){if(random()<.72)homePens++;if(random()<.72)awayPens++}
  state.shootout={home:homePens,away:awayPens};await log(`PÊNALTIS — ${homePens} a ${awayPens}.`)
}
